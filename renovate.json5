{
  // Default config + GitHubActionDigests helper
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:base",
    "helpers:pinGitHubActionDigests"
  ],

  // Add reviewers and custom labels to pull requests
  "reviewers": ["ThorpeJosh"],
  "labels": ["dependencies"]
}
